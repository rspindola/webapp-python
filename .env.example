# Porta exposta na rede (acesso: http://ip-do-servidor:APP_PORT)
APP_PORT=80

# Caminho raiz dos dados no servidor.
# O container monta esse caminho identicamente, entao usuarios
# digitam os caminhos reais sem adaptacao.
# Use / para montar o filesystem inteiro (mais simples)
DATA_ROOT=/

